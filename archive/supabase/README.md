# Archived Supabase files (pre-self-host)
Ghostforge used to store its data in Supabase Cloud. It is now fully
self-hosted (SQLite file + local audio folder), so these files are no longer
used. They are kept here only as a reference / rollback path:
- `SUPABASE_SETUP.sql`, `SUPABASE_MIGRATION_02.sql`, `SUPABASE_MIGRATION_03.sql` — the old cloud schema.
- `SUPABASE.md` — the old cloud-setup guide.
